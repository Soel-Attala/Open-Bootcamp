public class Main {
    public static void main(String[] args) {
       //que es la depuracion?
        // consiste en buscar programas o anomalias durante la ejecucion del programa.
        //los depuradores funcionan mediante puntos de ruptura.
        // Estos son una parte donde le decimos al programa que debe parar.

        // Como lo insertamos?
        // Pueden ser por linea de comando o usar el del entorno.

        //Ejemplo

        int suma = 0;
        suma = suma+15;
        System.out.println(suma);

        //si tenemos muchas funciones encadenadas podemos recurrir a la pila de llamadas en el depurador,
        // para de esta forma ver por donde ha pasado la ejecucion y encontrar el origen de el fallo.

        //tambien podemos poner condiciones para que se detenga o no la ejecucion usando los watch point



    }
}